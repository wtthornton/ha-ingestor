# OpenVINO Service Dependencies
# Phase 1: Containerized AI Models

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data Validation
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP Client
httpx==0.25.2

# Data Processing
pandas==2.1.4
numpy==1.26.2

# Simplified Model Stack (Standard models for compatibility)
# CPU-only PyTorch index (no CUDA - saves 8.5GB!)
--extra-index-url https://download.pytorch.org/whl/cpu

sentence-transformers==3.3.1  # Embeddings: all-MiniLM-L6-v2
transformers>=4.41.0,<5.0.0   # HuggingFace models (compatible with sentence-transformers)
torch                         # PyTorch CPU-only (1.5GB vs 10GB with CUDA)
sentencepiece                 # Required for T5 tokenizer
# Removed OpenVINO for now due to dependency conflicts

# Utilities
python-dotenv==1.0.0
tenacity==8.2.3

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
