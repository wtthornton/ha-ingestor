# Automation Miner - Community Knowledge Crawler
# Story AI4.1: Community Corpus Foundation

# HTTP Client (Context7 validated: async, retry, timeout)
httpx>=0.27.0

# HTML Parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Data Validation (Context7 validated: BaseModel, field validators)
pydantic>=2.8.0
pydantic-settings>=2.5.0

# API Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database (Context7 validated: async ORM)
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
alembic>=1.13.0

# Job Scheduling (Context7 validated: AsyncScheduler, CronTrigger)
apscheduler>=3.10.0

# YAML Parsing
pyyaml>=6.0

# Fuzzy String Matching (deduplication)
rapidfuzz>=3.0.0

# Logging
python-json-logger>=2.0.0

# Development
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-httpx>=0.30.0

