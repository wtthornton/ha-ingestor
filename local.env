# Local Development Configuration for ha-ingestor
# This configuration will work immediately for testing

# Home Assistant MQTT Configuration (using Docker Compose)
HA_MQTT_HOST=mqtt-broker
HA_MQTT_PORT=1883
HA_MQTT_USERNAME=tapphousemqtt
HA_MQTT_PASSWORD=Rom24aedslas!@
HA_MQTT_CLIENT_ID=ha-ingestor-docker

# Home Assistant WebSocket Configuration (using Docker Compose)
HA_WS_URL=ws://home-assistant:8123/api/websocket
HA_WS_TOKEN=disabled_for_local_testing

# InfluxDB Configuration (using Docker Compose)
INFLUXDB_URL=http://influxdb:8086
INFLUXDB_TOKEN=your_influxdb_token
INFLUXDB_ORG=myorg
INFLUXDB_BUCKET=ha_events

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=console

# Performance (conservative for local testing)
INFLUXDB_BATCH_SIZE=10
INFLUXDB_FLUSH_INTERVAL=5

# Advanced Features (DISABLED for deployment)
MQTT_ENABLE_PATTERN_MATCHING=false
MQTT_ENABLE_DYNAMIC_SUBSCRIPTIONS=false
MQTT_ENABLE_TOPIC_OPTIMIZATION=false

# Service Configuration
SERVICE_HOST=0.0.0.0
SERVICE_PORT=8000
